fullcalendar_loader:
    path: /fc-load-events
    defaults: { _controller: GlobalBundle:Calendar:loadCalendar }
    options:
        expose: true
        
updateInfo:
    path: /addevent
    defaults: { _controller: GlobalBundle:Calendar:updateData }
    options:
        expose: true
